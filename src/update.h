#include <stdio.h>
/*header file containing the update routines*/

void update_hares(double map[NX+2][NY+2] double hares[NX][NY],\
double pumas[NX][NY], double timeStep, double a, double k, double r)

void update_pumas(double pumas[NX][NY], int timeStep)
